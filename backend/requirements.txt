# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# ── Validación y Serialización ─────────────────────────────────────────────────
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0

# ── ORM y Base de Datos ────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.12.0
asyncpg>=0.29.0          # Driver async para PostgreSQL
psycopg2-binary>=2.9.0   # Driver sync (usado por Alembic CLI)

# ── Seguridad / Autenticación ──────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT encode/decode
passlib[bcrypt]>=1.7.4             # Hash de contraseñas con bcrypt
bcrypt<4.0.0                       # passlib no es compatible con bcrypt>=4.x
python-multipart>=0.0.6            # Necesario para Form() en FastAPI

# ── Sanitización ───────────────────────────────────────────────────────────────
bleach>=6.1.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=7.0.0
httpx>=0.24.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0            # Cobertura de tests — requerido por SonarQube
aiosqlite>=0.19.0           # Driver async SQLite para tests en memoria (sin necesitar PostgreSQL real)
greenlet>=3.0.0             # Requerido por SQLAlchemy async internamente
